    <% if flash[:notice] %>
      <div class="flash">
        <%= flash[:notice] %>
      </div>
    <% end %>

  <h4>Title</h4>
  <%= @book.title %>
  <h4>Body</h4>
  <%= @book.body %>

<%= link_to "edit", edit_book_path %>
<%= link_to "Back", books_path %>